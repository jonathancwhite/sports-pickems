{
  "name": "@callsheet/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsc --watch",
    "typecheck": "prisma generate && tsc --noEmit",
    "lint": "eslint src/",
    "db:generate": "prisma generate",
    "db:migrate": "sh -c 'DATABASE_URL=\"${DATABASE_URL_DIRECT:-$DATABASE_URL}\" prisma migrate deploy'",
    "db:migrate:dev": "sh -c 'DATABASE_URL=\"${DATABASE_URL_DIRECT:-$DATABASE_URL}\" prisma migrate dev'",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@callsheet/shared": "workspace:*",
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "prisma": "^6.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "prisma": {
    "schema": "prisma/schema.prisma",
    "seed": "tsx prisma/seed.ts"
  }
}
