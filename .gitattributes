# Generated by the TanStack Router Vite plugin on every dev run, always with
# LF. Without this, `core.autocrlf=true` rewrites it to CRLF on checkout and the
# plugin rewrites it back to LF, so the file shows as modified forever.
apps/web/src/routeTree.gen.ts text eol=lf
