# Database
# DATABASE_URL — runtime (API/worker); use pooler URL in production
# DATABASE_URL_DIRECT — direct connection for `prisma migrate` (port 5432)
DATABASE_URL=postgresql://callsheet:callsheet@localhost:5432/callsheet
DATABASE_URL_DIRECT=postgresql://callsheet:callsheet@localhost:5432/callsheet

# Clerk
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
CLERK_WEBHOOK_SECRET=

# API
PORT=3001
CRON_SECRET=
NODE_ENV=development
WEB_URL=http://localhost:5173

# Resend
RESEND_API_KEY=
EMAIL_FROM=noreply@callsheet.app

# App (Vite — prefix with VITE_)
VITE_API_URL=http://localhost:3001
VITE_CLERK_PUBLISHABLE_KEY=
VITE_APP_NAME=Callsheet
